numeros = [1, 2, 3 , 56, 12, 33]

numeros.sort() # Ordena del menor a mayor
numeros.sort(reverse=True) # Ordena del mayor al menor
sorted(numeros) # Crea una nueva lista pero ordenada, pero no afecta a la lista original
print(numeros)