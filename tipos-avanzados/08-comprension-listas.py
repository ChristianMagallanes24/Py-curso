usuarios = [
    ["Jose", 1],
    ["Pepe", 2],
    ["Jorge", 4],
    ["Martin", 3]
]

# nombres = []
# for usuario in usuarios:
#     nombres.append(usuario[0])
# print(nombres)

#MAP
# nombres = [usuario[0] for usuario in usuarios]
#FILTER
# nombres = [usuario for usuario in usuarios if usuario[1] > 2] # Filtrarlos
# nombres = [usuario[0] for usuario in usuarios if usuario[1] > 2] # Filtrarlos y transformarlos

# nombres = list(map(lambda usuario: usuario[0], usuarios)) Filtar
# menosUsuarios = list(filter(lambda usuario: usuario[1] > 2, usuarios)) Map
# print(menosUsuarios)